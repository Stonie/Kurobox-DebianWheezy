/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-powerpc)
GROUP ( /lib/powerpc-linux-gnu/libc.so.6 /usr/lib/powerpc-linux-gnu/libc_nonshared.a  AS_NEEDED ( /lib/powerpc-linux-gnu/ld.so.1 ) )
