#
# We would like to use just usr/bin/gettext but that's already taken.
#
gettext-base: script-with-language-extension usr/bin/gettext.sh
#
# gettext-base Provides libasprintf0c2, which is libasprintf0
# after a C++ transition, so it's correct.
#
gettext-base: package-name-doesnt-match-sonames libasprintf0
