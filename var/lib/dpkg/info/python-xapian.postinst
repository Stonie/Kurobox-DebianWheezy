#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/python-xapian.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-xapian
	if grep -qs '^python-xapian$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-xapian$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
